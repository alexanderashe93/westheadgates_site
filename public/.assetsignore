# Files inside public/ that must not be published as part of the website.
# Everything listed here is skipped when wrangler uploads the assets.

# Captions are read by the gallery build, not fetched by the browser — the
# text is already baked into images.json by the time the site deploys.
images/captions.json
