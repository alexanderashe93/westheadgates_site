{
  "name": "westhead-gates",
  "private": true,
  "type": "module",
  "description": "Westhead Gates Ltd brochure site. Static HTML on Cloudflare; the Worker handles the contact form.",
  "scripts": {
    "build": "node scripts/build-gallery.mjs",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.129.1"
  }
}
